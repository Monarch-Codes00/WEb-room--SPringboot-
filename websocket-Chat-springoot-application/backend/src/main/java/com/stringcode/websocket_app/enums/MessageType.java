package com.stringcode.websocket_app.enums;

public enum MessageType {
    JOIN,
    LEAVE,
    PING,
    SYSTEM,
    ROOM_PRESENCE,
    ONLINE_USERS,
    CHAT,
    LOGIN,
    OFFER,
    ANSWER,
    ICE_CANDIDATE,
    CALL_REQUEST,
    CALL_RESPONSE,
    CALL_HANGUP
}